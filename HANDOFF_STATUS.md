# Handoff status

This repository is a clean standalone extraction of the interactive cooling sandbox.

Validated locally:

- `npm ci` — passed with zero reported vulnerabilities
- `npm run check` — passed
- `npm run demo:validate` — passed
- `npm run build` — passed
- Vite development root and the production `npm run start` root — passed

No website pages, navigation, backend routes, data stores, stories, contact forms, or research content are included.
